spring:
  application:
    name: eureka-server
  cloud:
    config:
      uri: http://localhost:8071
  config:
    import: "optional:configserver:"

eureka:
  server:
    wait-time-in-ms-when-sync-empty: 5